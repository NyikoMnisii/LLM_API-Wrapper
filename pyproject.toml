[project]
name = "agrilite-api"
version = "0.1.0"
description = "Modular FastAPI backend for the Agrilite AI agronomist platform."
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
